# 🎙️ Голосовой ввод текста

Фоновая программа для распознавания речи и автоматического ввода текста в активные поля на Windows.

## ✨ Возможности

- 🎤 Распознавание русской и английской речи (в том числе смешанной)
- 🤖 Умное комбинирование результатов через DeepSeek AI
- 🔌 Офлайн распознавание через Vosk (две модели одновременно)
- 🌐 Опциональная поддержка Google Speech-to-Text
- ⌨️ Автоматический ввод текста в активное окно
- 🔄 Умное восстановление фокуса после записи
- ⚡ Работает в фоне без UI
- 🔥 Горячая клавиша для быстрого доступа
- 🔔 Звуковые сигналы при старте и окончании записи
- ⚙️ Гибкая настройка через config.yaml

## 📦 Установка

1. Установите Python 3.8+
2. Создайте виртуальное окружение:
```bash
python -m venv .venv
.venv\Scripts\activate
```
3. Установите зависимости:
```bash
pip install -r requirements.txt
```
4. Скачайте модели Vosk:
   
   **Маленькие модели** (рекомендуется для начала):
   - Русская: [vosk-model-small-ru-0.22](https://alphacephei.com/vosk/models/vosk-model-small-ru-0.22.zip) (~45 MB)
   - Английская: [vosk-model-small-en-us-0.15](https://alphacephei.com/vosk/models/vosk-model-small-en-us-0.15.zip) (~40 MB)
   - Распакуйте в `model/model-ru` и `model/model-en`
   
   **Большие модели** (лучшее качество):
   - Русская: [vosk-model-ru-0.42](https://alphacephei.com/vosk/models/vosk-model-ru-0.42.zip) (~2.5 GB)
   - Английская: [vosk-model-en-us-0.22](https://alphacephei.com/vosk/models/vosk-model-en-us-0.22.zip) (~1.8 GB)
   - Или запустите `download_large_models.cmd`

5. (Опционально) Добавьте DeepSeek API ключ в `.env` для улучшенного комбинирования:
```
DEEPSEEK_API_KEY=ваш_ключ
```

## 🚀 Запуск

```bash
.venv\Scripts\activate
python main.py
```

## 🎯 Использование

### Горячая клавиша
- **Alt+`** - Начать/остановить запись
- **Ctrl+C** - Выход из программы (в консоли)

### Как это работает
1. Нажмите **Alt+`**
2. Услышите два коротких звука "ding-ding" - запись началась
3. Говорите в микрофон (русский или английский)
4. Нажмите **Alt+`** снова для остановки записи
5. Услышите два звука "dong-dong" - запись остановлена
6. Текст автоматически вставится в активное окно

## 🌍 Языки и смешанная речь

Программа использует **две модели Vosk одновременно**:
- 🇷🇺 Русская модель - для русских слов
- 🇺🇸 Английская модель - для английских слов

**DeepSeek AI** умно комбинирует результаты:
- Выбирает правильные слова по уверенности (confidence)
- Заменяет транслитерацию ("хеллоу") на латиницу ("hello")
- Убирает ошибочные распознавания
- Предпочитает русский язык по умолчанию

**Примеры:**
- "привет" → "привет" ✓
- "hello world" → "hello world" ✓
- "привет my friend" → "привет my friend" ✓

## ⚙️ Требования

- Микрофон
- Windows 10/11
- ~85 MB для двух моделей Vosk (офлайн режим)
- Интернет для DeepSeek AI (опционально, но рекомендуется)
- Интернет для Google Speech-to-Text (опционально)

## 🔧 Настройка

### Движок распознавания

Отредактируйте `config.yaml`:

```yaml
# Движок: "vosk" (офлайн, две модели + AI) или "google" (онлайн)
engine: "vosk"

vosk:
  model_path: "model/model-ru"      # Русская модель
  model_path_en: "model/model-en"   # Английская модель
  sample_rate: 16000
```

### API ключи

Создайте файл `.env`:

```bash
# DeepSeek AI для умного комбинирования (рекомендуется)
DEEPSEEK_API_KEY=ваш_ключ

# Google Speech-to-Text (опционально)
GOOGLE_API_KEY=ваш_ключ
```

### Структура папок

```
microphone/
├── model/
│   ├── model-ru/      # Русская модель
│   │   ├── am/
│   │   ├── conf/
│   │   ├── graph/
│   │   └── ...
│   └── model-en/      # Английская модель
│       ├── am/
│       ├── conf/
│       └── ...
├── main.py
└── config.yaml
```
